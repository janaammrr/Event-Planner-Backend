# Copy this file to .env and fill in values as needed

# MySQL connection (optional during development)
DB_USER=
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=EventPlanner

# JWT secret used to sign tokens
JWT_SECRET=dev_secret_change_me
